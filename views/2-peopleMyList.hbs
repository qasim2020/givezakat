<h3 style="font-size:120%;color:#173627;">People added by me</h3>

<div class="content-grid pt-md-2 pt-1">

  {{#unless addedbyme}}
  <h2 class="w-100 text-center" style="
  grid-column: 1/-1;
  font-size: 91%;
  color: brown;
  align-self: center;
  ">Did not find any data for this filter !</h2>
  {{/unless}}


  {{#each addedbyme}}

  {{>cards}}

  {{/each}}

</div>

{{#if addedbyme}}

  <button type="{{query.type}}" expression="{{query.expression}}" class="load-more btn btn-primary d-flex align-items-center" type="button" name="button" style="margin:2rem auto; display: block">Load More</button>
{{/if}}

<h3 style="font-size:120%;color:#173627;">People given zakat by me</h3>

<div class="content-grid pt-md-2 pt-1">

  {{#unless paidbyme}}
  <h2 class="w-100 text-center" style="
  grid-column: 1/-1;
  font-size: 91%;
  color: brown;
  align-self: center;
  ">Did not find any data for this filter !</h2>
  {{/unless}}

  {{#each paidbyme}}

    {{>cards}}

  {{/each}}

</div>

{{#if paidbyme}}
<button type="{{query.type}}" expression="{{query.expression}}" class="load-more btn btn-primary d-flex align-items-center" type="button" name="button" style="margin:2rem auto; display: block">Load More</button>
{{/if}}
